name: 'fuzzyocto-action'
description: 'label management for issues'
author: 'muachilin'
inputs:
  github-token:
    description: 'Github token'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'

branding:
  icon: cloud-snow
  color: red
